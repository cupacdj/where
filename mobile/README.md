# NightOut Mobile (React Native + Expo)

This is the React Native + Expo client for NightOut.

## Quick start

```bash
cd mobile
cp .env.example .env   # set API_BASE_URL
npm install
npm run start
```

Open the app in Expo Go and make sure your backend (`../backend`) is running on the URL from `.env`.
